<?php
// ไฟล์นี้ใช้สำหรับตั้งค่าฐานข้อมูลบนเครื่อง XAMPP ของคุณเอง
// Git จะละเว้น (Ignore) ไฟล์นี้ ดังนั้นเวลา Push/Pull จะไม่ไปทับของ Server จริง

$host = '127.0.0.1';
$db   = 'tfs_db';
$user = 'root'; // User ของ XAMPP มักจะเป็น root
$pass = '';     // XAMPP ปกติจะไม่มีรหัสผ่าน (ปล่อยว่างไว้)
